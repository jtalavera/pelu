{
  "name": "cursor-poc-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0 <21"
  },
  "scripts": {
    "test": "playwright test --pass-with-no-tests",
    "test:headed": "playwright test --headed",
    "test:with-backend": "E2E_WITH_BACKEND=1 playwright test",
    "test:with-backend:headed": "E2E_WITH_BACKEND=1 playwright test --headed",
    "test:with-evidence": "bash ../scripts/run-e2e-with-evidence.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0"
  }
}
