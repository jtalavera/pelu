# implement-user-story

Move to main branch, pull latest changes, create a feature branch and work on the user story indicated by the user. 
Once finished, commit, push, and create a PR targeting the main branch. 
Make sure that steps in CI/CD, such as linting, unit testing, etc, work before pushing to the repository
